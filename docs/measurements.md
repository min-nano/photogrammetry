# 実測の記録（事実の保管庫）

**この文書は事実だけを置く。設計上の主張・方針・推奨は書かない。**

PR #11〜#17 は設計案ごと close したが、**そこで得た実測は設計の当否とは独立に
有効**なので、案から切り離してここに保存する。以後、設計文書はこの文書を
**引用する側**であって、この文書が設計を根拠づけることはあっても、設計の変更で
この文書の中身が変わることはない。

## 0. 読み方の規約

- **数値と観測だけを書く。** 解釈が必要なところは `〔解釈〕` と明示して分離する。
- 各節に**出所**（どの PR / どのスクリプト / どの run）を書く。
- **写真そのものは含まない。** 数値・ファイル名・統計だけなので共有できる。
- 断りが無い限り、対象は**同一の建築現場の写真 1424 枚**（2026-08-03 取得。
  外部・内部・床下・小屋裏を含む。撮影者の申告では結節点が少なく、床下は
  他と完全に切り離されている）。

**close した PR とブランチ**（ブランチは消していないので道具は取り出せる）:

| PR | 何をした案か | ブランチ |
| --- | --- | --- |
| #11 | 視覚特徴（feature print）で同じ場所を見分ける | `claude/phase-2-room-classification-ydsmg9` |
| #12 | 画素の重なりの実測でグループを決める | `claude/object-capture-preprocessing-k6aawg` |
| #13 | 仕分けを「重なりつきの被覆」へ作り替える | `claude/image-clustering-redesign-czfxyw` |
| #14 | 窓を作る→投げる→姿勢を返す試行スクリプト | `claude/photo-clustering-trial-i8i5q5` |
| #15 | ANE コンパイル失敗の調査 | `claude/object-capture-ane-error-f27g79` |
| #16 | 手で作った正解で指標を採点する | `claude/photogrammetry-clustering-manual-bue9eu` |
| #17 | Object Capture だけで大域姿勢を作れるか | `claude/object-capture-covisibility-graph-12v88x` |

**main から撤去した文書**（内容は git の履歴に残っているので、下のコマンドで
いつでも読める。main の作業ツリーに置いておく理由が無いだけ）:

| 文書 | 何が書いてあったか | 取り出し方 |
| --- | --- | --- |
| `docs/design-preprocess-merge.md` | 建築物向けプリ／ポストプロセス設計。`sort` の設計（PR #2 / #8）、`merge` の設計と USD 連携の検証、進捗表示（フェーズ 0）、撮影ガイド | `git show 44c0ec2:docs/design-preprocess-merge.md`（PR [#2](https://github.com/min-nano/photogrammetry/pull/2) / [#8](https://github.com/min-nano/photogrammetry/pull/8)） |
| `docs/design-loose-clustering.md` | 共視グラフから窓を育てる設計 | PR [#13](https://github.com/min-nano/photogrammetry/pull/13) / [#14](https://github.com/min-nano/photogrammetry/pull/14)（main には入っていない） |
| `docs/design-oc-only-covisibility.md` | OC だけで大域姿勢を作る思考実験 | PR [#17](https://github.com/min-nano/photogrammetry/pull/17)（同上） |
| `docs/design-manual-truth.md` | 手作業の正解づくり | PR [#16](https://github.com/min-nano/photogrammetry/pull/16)（同上） |
| `docs/investigate-ane-compile-failure.md` | ANE コンパイル失敗の調査 | PR [#15](https://github.com/min-nano/photogrammetry/pull/15)（同上） |

**撤去した文書のうち、いま必要な中身はこの文書と `docs/roadmap.md` へ移してある**
（`merge` の確定事項は roadmap A-3、USD と `sort` の検証結果は本書 §3.14〜§3.16）。

主な計測スクリプト: `scripts/measure-ordering.swift`（視覚特徴）・
`scripts/measure-poses.swift`（Object Capture への投入と姿勢）・
`scripts/measure-truth.swift`（手作業の正解との照合）・
`scripts/measure-ane.swift`（ANE 失敗）。いずれも上記ブランチにある。

---

## 1. 現場そのものの事実

出所: #13 / #14（`measure-ordering.swift`）

| 項目 | 値 |
| --- | --- |
| 写真の総数 | **1424 枚** |
| EXIF 撮影時刻あり | **1337 枚** |
| EXIF 撮影時刻なし | **87 枚**（うち 85 枚はファイル名に連番） |
| 機種名・焦点距離・GPS がすべて欠落 | **89 枚** |
| 撮影間隔が 5 秒以内 | **96%** |
| 5 分以上のギャップ | **8 箇所** |
| 35mm 換算焦点距離の種類 | **14 種**（13mm が 413 枚 = 31%） |

**EXIF の有無と姿勢の付きやすさ**（出所: #17 §11.1）

| ファイル名 | 姿勢が付いた割合 |
| --- | --- |
| ハッシュ名（EXIF なし）25 枚中 | **1 枚（4%）** |
| `IMG_` 名 | **62.5%** |

## 2. 視覚特徴（Vision feature print）の測定

出所: #13 / #14（`measure-ordering.swift`）

```
result=go step1=ok ratio=0.539 recall1@30=0.88 lift=39.2 ordering=exif foreign=2 n=1337
```

| 測ったもの | 値 | 無作為の場合 |
| --- | --- | --- |
| 隣り合う写真の距離 ÷ 無関係な組の距離 | **0.539** | 1.0 |
| 撮影順 ±1 が距離の上位 30 件に入る割合 | **0.88** | 0.02 |
| 上の比（lift） | **39.2 倍** | 1 |
| 無関係な組の 90% の距離に達する撮影順の隔たり | **34 枚** | — |

**隔たりごとの距離**（単調に増える）: 0.242 → 0.307 → 0.341 → … → 0.456
（無関係な組は 0.450）

**距離の分布**（0.05 刻み・出所 #11）

```
0.00-0.15 に 130 組、0.15-0.30 に 3706 組、0.30-0.45 に 2099 組
```

連続撮影した同じ壁のペアが 0.2 前後、別の部屋のペアが 0.3 前後。

〔解釈〕この 2 つが同時に成り立つ。**順位としては強く効く（39 倍）が、絶対値の
閾値としては分離しない（帯に潰れている）。**

## 3. Object Capture の挙動

### 3.1 窓が通るか通らないかの地図（撮影順で切った 100 枚の窓）

出所: #13 / #14（`measure-poses.swift`）。数字は姿勢が付いた枚数。

| start | normal/unordered | high/unordered | high/sequential | + ブレ除去 |
| --- | --- | --- | --- | --- |
| 0 | error 6 | **ok 87** | ok 79 | — |
| 150 | error 6 | error 6 | **ok 41** | — |
| 300 | error 6 | error 6 | **ok 25** | — |
| 400 | error 6 | — | error 6 | **ok 43**（25% 落とす） |
| 450 | error 6 | error 6 | error 6 | **ok 24**（10% 落とす） |
| 600〜1050 | ok 42〜65 | — | ok 37〜73 | — |

- start=0 は 100 枚では通らず、**30〜50 枚なら通った**。
- ハードウェアの入力上限は **1000 枚**。

### 3.2 共視グラフから作った窓（EXIF 不使用）

出所: #14（`trial-clustering.sh`）

- 15 窓を投げて **12 窓が通った**。
- 落ちた 4 窓を `high` で投げ直した結果:

| 窓 | 投入（ブレ除去後） | 姿勢 | 結果 | 位置合わせの時間 |
| --- | --- | --- | --- | --- |
| 04 | 約 234 | 0 | error 6 | 229 秒 |
| 06 | 約 207 | 0 | error 6 | 219 秒 |
| 07 | 約 153 | **92** | ok | 649 秒 |
| 13 | 約 72 | 0 | error 6 | **51 秒** |

- 最良の窓: **212 枚（ブレ除去後 190 枚）を投げて 180 枚に姿勢（94.7%）**。
- 別の窓（`r004-window-07`・190 枚）: `sequential` 177 枚 / `unordered` 186 枚。

### 3.3 `sampleOrdering` の A/B（同じ 190 枚の窓）

出所: #14

| | `sequential` | `unordered` |
| --- | --- | --- |
| 姿勢 | 180 枚 | **183 枚**（skipped 10 → 7） |
| 位置合わせ | 791 秒 | 800 秒（+1%） |
| 点群 | 133,347 点 | 129,367 点 |

**ただし §3.1 の start=150 / 300 は `high/unordered` で error 6・
`high/sequential` で通っている。** A/B が取れているのは屋外主体の通りやすい窓のみ。

### 3.4 段階ごとの所要時間

**モデルを要求しなかったとき**（`high`・100 枚・全 516 秒）

```
preProcessing 1 → imageAlignment 9 → pointCloudGeneration 499
→ meshGeneration 507 → textureMapping 510 → optimization 512
```

位置合わせが 95%、メッシュ + テクスチャ + 最適化は合計 13 秒（2.5%）。

**モデルを要求したとき**（190 枚）

| 段階 | その段階に費やした時間 | 割合 |
| --- | --- | --- |
| preProcessing | 11 | 1% |
| **imageAlignment** | **791** | **49%** |
| pointCloudGeneration | 73 | 4% |
| **meshGeneration** | **552** | **34%** |
| textureMapping | 173 | 11% |
| optimization | 13 | 1% |

`--models-out` を外すと `meshGeneration` + `textureMapping` は
**725 秒 → 8 秒**、ピークメモリは **14.4GB → 4.7GB**。

### 3.5 メモリ

| 条件 | ピーク |
| --- | --- |
| 190 枚・モデルあり | **14.4 GB** |
| 190 枚・モデルなし | **4.7 GB** |
| 427 枚・位置合わせ | 3.6 GB（**8.4 MB/枚**） |
| 190 枚・点群の段 | 25 MB/枚 |

### 3.6 姿勢と点群

出所: #14

- 点群は **133,347 点**（2.0MB）。取得の追加コストは **73 秒（全体の 4%）**。
- **点群と姿勢は同じ座標系**:

```
点群の範囲 (-7.11, -0.85, -8.23) 〜 (7.30, 3.48, 7.40)  対角 21.70
カメラの範囲 (-5.92, 0.86, -5.45) 〜 (3.13, 1.99, 5.17)  対角 14.00
カメラから点群中心まで: 中央値 3.80（点群の対角の 18%）
カメラ座標系での点: 前方(-Z) 71% / 後方 28%
前方の点の光軸からの角度: 中央値 44°・下位 25% は 30°
```

- 別の実行での座標系の妥当性（#17 §11.1）: **カメラ高さの中央値 1.24 m・
  対角 16.16 m**。

### 3.7 成否と相関したもの / しなかったもの

出所: #14

**相関しなかった**（窓の中身の統計）

| | 鋭さ中央値 | 鋭さ下位 10% | 隣接距離 | レンズ数 |
| --- | --- | --- | --- | --- |
| error 6 の区間 | 528 | 272 | 0.247 | 3.75 |
| 成功した区間 | 638 | 290 | 0.244 | 2.88 |

**相関しなかった**（グラフの連結性）

```
連結 1.00       投げた 4 回 / 通った 0 回
連結 1.00 未満  投げた 12 回 / 通った 4 回
```

**相関した**（窓の写真が撮影順でいくつの塊に散っているか）

| 窓 | 撮影順の塊 | 最大の塊 | 結果 |
| --- | --- | --- | --- |
| w1 | 6 | 0.54 | **ok 180** |
| w7 | 5 | 0.63 | **ok 177 / 186** |
| w8 | 5 | 0.89 | **ok 185 / 159** |
| w5 | **18** | 0.18 | error 6 |
| w4 | **17** | 0.17 | error 6 |
| w27 | **11** | 0.24 | error 6 |
| w20 | **16** | 0.29 | error 6 |
| w14 | **14** | 0.27 | error 6 |

通った窓は 5〜6 か所からまとまって来ており、落ちた窓は 11〜18 か所に散っている。
**通った 4 回はいずれも屋外主体の窓。**

### 3.8 失敗は早く落ちる

出所: #14

| | 所要 |
| --- | --- |
| 落ちた窓 | **257〜385 秒** |
| 通った窓 | **520〜870 秒** |

### 3.9 窓の大きさは成否を決めない

- 72 枚の窓が 51 秒で error 6。
- 153 枚の窓が 649 秒かけて ok（姿勢 92）。

### 3.10 異物を混ぜても、通る窓は通る

出所: #17 §11.1

**190 枚の既知の窓 ＋ 別成分の 134 枚 = 324 枚**（異物 41%）を投入。

| | 値 |
| --- | --- |
| 姿勢が付いた枚数 | **188**（元の窓のみ。1 枚も失っていない） |
| 異物 134 枚に付いた姿勢 | **0 枚** |
| error 6 | **出ていない** |
| 得られた否定の組 | 25,192 組 |

### 3.11 一様な間引きは密度で落ちる（枚数では説明できない）

出所: #17 §11.2 / §11.4

| | 全密度 | 1/3 に間引き |
| --- | --- | --- |
| 小（56〜190 枚） | **ok**（190 → 姿勢 188） | **error 6**（56 枚・126 秒） |
| 大（324〜427 枚） | **ok**（324 → 姿勢 188） | **error 6**（427 枚・808 秒） |

### 3.12 姿勢をグラフへ返す反復

出所: #14

| 巡 | 見比べられた辺 | 確定 | 除去 | 除去率 |
| --- | --- | --- | --- | --- |
| 003 | 51 | 3 | 21 | 41% |
| 004 | 707 | 638 | 40 | 5.7% |
| 005 | 736 | 664 | 33 | 4.5% |
| 006 | 910 | 846 | 30 | 3.3% |
| 007 | 958 | 856 | 57 | 5.9% |

- 初回の内訳: 共通近傍フィルタ後の辺 4265 / 確定 635 / 除去 19。
  **判定が届いたのは 654 本だけ**（残りは両端が同じ窓に入っておらず未判定）。
- 5 巡ぶんの除去は合計 **181 本 / 4284 本 = 約 4%**。
- **次数 0 の写真が 155 枚（11%）。**
- 窓 1 個ぶんの姿勢で判定が届く範囲は 19.7%、2 個で 34.1%。

### 3.13 窓の再生成（合成データ）

出所: #14（`--compare-windows`）

| 条件 | 前の巡と同じ窓 |
| --- | --- |
| フィードバック無しで作り直す | **6 / 6 個がそのまま**（Jaccard 1.00） |
| 姿勢を反映して作り直す（辺 24 本除去） | **5 / 6**。辺が消えた領域の窓だけ作り直された（0.64） |

### 3.14 API が返すもの / 返さないもの（実機の SDK で確認）

出所: #14 / PR #2（ci-debug の `swiftc -typecheck` で実在を確認）

- `Pose`（`translation` / `rotation` / `transform`）は macOS 14+ で返る。
- `PointCloud`（`position` + `color`）は macOS 13+ で返る。
- **投影行列（内部パラメータ）は返らない。** `intrinsics` / `calibrationData` は
  `PhotogrammetrySample`（**入力**側・macOS 15+）にあり、OC の推定値は読めない。
- `MDLAsset` は **usdz を書き出せない**（`canExportFileExtension("usdz")` が false）。
  `SCNScene.write(to:)` は書ける。
- SceneKit 経由の往復で劣化なし（頂点 12,608 / プリミティブ 24,999 /
  マテリアル 1 が入出力で一致。ファイルサイズ 2,023,159 → 2,022,755 バイト）。

### 3.15 USD の書き出しと取り込みの実測

出所: PR #2（`scripts/make-usd-samples.sh` / `scripts/usdz-roundtrip.swift`）。
run: <https://github.com/min-nano/photogrammetry/actions/runs/30724126241>
取り込み側は Vectorworks。

| 方法 | usdz 書き出し | テクスチャ |
| --- | --- | --- |
| ModelIO `MDLAsset.export(to:)` | **不可**（`canExportFileExtension("usdz")` が false） | — |
| SceneKit `SCNScene.write(to:)` | **可** | zip 内に同梱される |
| 自前で `.usda` を書く（外部参照方式） | （usdz ではない） | **落ちる**（無地になる） |

**SceneKit で書いた単一 usdz を Vectorworks で開いた結果**

| 確認項目 | 結果 |
| --- | --- |
| テクスチャ（赤／青のチェッカー） | **OK**（表示される） |
| 単位系（1 USD 単位 = 1 m） | **OK**（赤い立方体が 1 辺 1m） |
| 相似変換のスケール（0.5 倍） | **OK**（青い立方体が 1 辺 500mm） |
| 相似変換の回転・並進（Y 軸 45°・X 方向 3.0） | **OK** |
| 上方向（USD は Y-up / Vectorworks は Z-up） | **OK**（自動変換される） |
| メッシュの寸法 | **正確**（幅・奥行き・高さとも 1000mm、原点中心で -500〜+500） |

**外部参照方式（`.usda` から usdz を `references`）**: 参照の解決・相似変換・prim
名の保持はすべて OK だが、**テクスチャだけ NG**。生成時の警告と一致する。

```
Failed to resolve reference @0/texgen_0.png@ with computed asset path @0/texgen_0.png@
```

〔解釈〕SceneKit が書く usdz はテクスチャをパッケージ内パス（`0/texgen_0.png`）で
参照する。単体で開けば解決できるが、外側のレイヤから参照されると解決できない。
**Object Capture が書いた usdz を参照した場合に同じことが起きるかは未検証。**

**ノード名の伝わり方**

```
scene_reloaded-3   （グループ。ファイル名由来）
  └ Geom           （Apple の USD エクスポータが挟む中間層）
      └ box_a-2    （メッシュ。SceneKit のノード名が届いている）
```

- ノード名は届く。ただし**重複名回避のサフィックス（`-2`）が付く**ので、
  名前で機械的に振り分けるなら**前方一致**で判定すること。
- **中間に `Geom` グループが挟まる**ので、取り込み後にメッシュへ到達するには
  グループを 2 回解除することになる。
- テクスチャは表示されるが、Vectorworks の「マテリアルを使用」はオフだった
  （メッシュのテクスチャとして入っており、マテリアルとしては割り当てられていない）。

### 3.16 `sort` の読み取り経路の確認（合成写真）

出所: PR #8（`scripts/make-sort-samples.swift`）。
run: <https://github.com/min-nano/photogrammetry/actions/runs/30742799841>

入力は「部屋 A 24 枚 → 12 分の移動 → 部屋 B 24 枚」で、A に濃淡のほとんど無い
（＝ブレ相当の）写真を 3 枚混ぜたもの。

| 確認項目 | 結果 |
| --- | --- |
| EXIF の読み取り（時刻・GPS・高度・方位・露出） | **OK**（coverage すべて 1.0） |
| ブレの自動判定 | **OK**（中央値 516 に対し閾値 230。濃淡の無い 3 枚だけ落ちた） |
| 時刻ギャップでの分割 | **OK**（21 枚 + 24 枚の 2 グループ） |
| 重複付き分割 | **OK**（共有 15 枚が両方のフォルダに実在） |
| 視点の散らばり | **OK**（`viewpointSpread` = 1.0） |
| `--dry-run` | **OK**（出力フォルダ自体が作られない） |

## 4. 棄却された方法の実測

### 4.1 画素の相関による重なり判定（#12）

| | ① 全体の相関 | ② ブロックごとの局所一致 |
| --- | --- | --- |
| 確かめた組 | 7,701 | 16,224 |
| 重なっていると判定 | 2,423 | **63** |
| **骨格の的中率** | **45%** | **1.1%** |
| どの写真とも重ならない | 49 枚（4%） | **1,265 枚（93%）** |
| 次数の分布 | 平均 3.56 | ほぼ全部 0 |
| 仕分けの結果 | 17 グループ | **1 枚のグループ 1 つ＋未割当 1,360 枚** |

②のインライア率は 20 分割の最初の 1 つ（0.00〜0.05）に 14,648 組が入った。

〔解釈〕視差のある 2 枚を揃える 2 次元変換は存在せず、探索窓（長辺の 8% = 38 画素）
は手持ち歩き撮りの視差に届かない。

### 4.2 視覚特徴の閾値による仕分け（#11）

- 近傍距離の中央値を基準にしても、`IMG_4554〜4660` の連続した塊に
  **700 枚離れた `IMG_3814〜3852` が 7 枚残った**。
- 分布に**谷が出ない**（§2 の帯に潰れた分布）。

### 4.3 大域的な並べ替え（フィードラーベクトル・#13）

実データ（相互 12 近傍・辺 5128 本）

| 並び | 隣り合う組の距離の中央値 |
| --- | --- |
| 撮影順（EXIF） | **0.242** |
| フィードラー並べ替え | **0.388** |
| 無関係な組 | 0.450 |

撮影順との一致は **13%**。

**真の順序が分かる合成グラフでの切り分け**（`simulate-seriation.py`）

| グラフの汚れ | 隣接一致 | 共通近傍フィルタ後 |
| --- | --- | --- |
| きれい | **0.99** | 0.99 |
| 孤立した空似 20 本（全 5356 本の 0.4%） | **0.45** | **0.99** |
| 孤立した空似 200 本 | 0.22 | 0.99 |
| **かたまりの空似 4 組** | 0.70 | **0.70**（フィルタが効かない） |

### 4.4 窓の成長で被覆を見ない規則（`--grow-full`・#14）

| | 2 段階（既定） | `--grow-full` |
| --- | --- | --- |
| 窓の数 | 14 | 29 |
| はぐれ | 12〜62 枚 | **0〜5 枚** |
| 連結 | 0.52〜0.96 | **大半が 1.00** |
| 延べ枚数 | 1.24 倍 | 4.2 倍 |

**指標は良くなったが、投げた 3 窓はすべて error 6**（w27: 200 枚・連結 1.0000・
芯 0.9900 で 322 秒。w20: 201 枚で 328 秒。w14: 200 枚で 313 秒）。

## 5. 環境要因

### 5.1 ANE モデルキャッシュのコンパイル失敗

出所: #14 / #15

```
E5RT encountered an STL exception. msg = MILCompilerForANE error:
failed to compile ANE model using ANEF. Error=_ANECompiler : ANECCompile() FAILED.
```

- 場所: `~/Library/Caches/<実行体名>/com.apple.e5rt.e5bundlecache`
- **この印が出ても完走することがある。** 出ただけでは失敗ではない。
- **一度通った窓を投げ直したら姿勢 186 枚で通った**（環境ではないことの切り分け）。
- 潰した候補: **Xcode のバージョン**（ANE モデルも ANECompiler も OS 側にあり、
  どの Xcode でビルドしてもコンパイルされる中身は変わらない）、
  **OS / デバイスターゲット**（宣言は `Package.swift` / `Info.plist` / ビルド /
  README の 4 か所で揃っている）。
- 未決の候補: **メモリ不足**（`scripts/measure-ane.swift` で測る道具まで作った）。

### 5.2 CI の Xcode

出所: #15。当時 3 本のワークフローすべてが **Xcode 16.2（macOS 15.2 SDK）に固定**
されており、`macos-15` ランナーの既定（16.4）より 3 世代前だった。
ユニバーサル（arm64 + x86_64）のリリースビルドが Xcode 26.6 / macos-26 でも通る
ことは ci-debug で確認済み。

## 6. 手作業の正解づくり（#16・道具のみ）

**正解そのものは作られていない**（人の作業が要る）。道具（`measure-truth.swift`・
`truth-selftest.sh`）は動く状態で `claude/photogrammetry-clustering-manual-bue9eu`
にある。見積りは 1424 枚で 1〜2 時間。

## 7. この文書に入れていないもの

- **PR #19 の数値実験**（`scripts/simulate-oc-sampling.py`）は模型の出力であって
  実測ではないので、ここには入れない。`docs/design-oc-only-sampling.md` にある。
- 各 PR の**設計上の主張**（窓の作り方・サンプリング方針・撤回の経緯）。
  それらは close した PR の中に残っている。
